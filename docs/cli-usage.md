# CLI Usage

Run a DevLang file:

```bash
node devlang.js -f program.dev
```

Run inline code:

```bash
node devlang.js -e "bolo 'Hello'"
```

Flags:

* `-f` : Run a file
* `-e` : Execute inline code
